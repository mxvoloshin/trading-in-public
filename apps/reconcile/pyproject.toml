[project]
name = "trade-reconcile-app"
version = "0.0.1"
description = "Planned-versus-actual reconciliation app entrypoint."
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
