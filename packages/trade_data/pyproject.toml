[project]
name = "trade-data"
version = "0.0.1"
description = "Market data ingestion and normalization boundaries."
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
