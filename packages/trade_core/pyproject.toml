[project]
name = "trade-core"
version = "0.0.1"
description = "Shared project-owned trading domain primitives."
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
